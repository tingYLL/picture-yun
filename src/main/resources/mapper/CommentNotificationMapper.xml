<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jdjm.jdjmpicturebackend.mapper.CommentNotificationMapper">

    <resultMap id="BaseResultMap" type="com.jdjm.jdjmpicturebackend.model.entity.CommentNotification">
        <id property="id" column="id" jdbcType="BIGINT"/>
        <result property="userId" column="userId" jdbcType="BIGINT"/>
        <result property="commentId" column="commentId" jdbcType="BIGINT"/>
        <result property="pictureId" column="pictureId" jdbcType="BIGINT"/>
        <result property="notificationType" column="notificationType" jdbcType="INTEGER"/>
        <result property="title" column="title" jdbcType="VARCHAR"/>
        <result property="content" column="content" jdbcType="LONGVARCHAR"/>
        <result property="triggerUserId" column="triggerUserId" jdbcType="BIGINT"/>
        <result property="isRead" column="isRead" jdbcType="INTEGER"/>
        <result property="isDelete" column="isDelete" jdbcType="TINYINT"/>
        <result property="createTime" column="createTime" jdbcType="TIMESTAMP"/>
        <result property="updateTime" column="updateTime" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="Base_Column_List">
        id,userId,commentId,pictureId,notificationType,title,content,
        triggerUserId,isRead,isDelete,createTime,updateTime
    </sql>

    <update id="markAsRead">
        UPDATE comment_notification
        SET isRead = 1,
            updateTime = NOW()
        WHERE id = #{id}
        AND userId = #{userId}
        AND isDelete = 0
    </update>

    <update id="batchMarkAsRead">
        UPDATE comment_notification
        SET isRead = 1,
            updateTime = NOW()
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        AND userId = #{userId}
        AND isDelete = 0
    </update>

    <select id="getUnreadCount" resultType="java.lang.Long">
        SELECT COUNT(*)
        FROM comment_notification
        WHERE userId = #{userId}
        AND isRead = 0
        AND isDelete = 0
    </select>

</mapper>